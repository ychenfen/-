# 放进已有站点（带 HTTPS）的 server { } 里，放在 location / 之前。
location = /dy {
    return 301 /dy/;
}

location /dy/ {
    proxy_pass http://127.0.0.1:8765/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_read_timeout 90s;
    client_max_body_size 64k;
}
